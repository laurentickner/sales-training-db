{
  "name": "live-coach",
  "version": "0.1.0",
  "private": true,
  "description": "Live sales-call copilot — Recall.ai bot + Cloudflare Worker + Durable Object + phone view. Pairs with the static SPA in ../app.",
  "scripts": {
    "dev": "wrangler dev",
    "dev:remote": "wrangler dev --remote",
    "tail": "wrangler tail",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250407.0",
    "typescript": "^5.5.4",
    "wrangler": "^3.114.0"
  }
}
